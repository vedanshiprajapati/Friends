export const publicRoutes = ["/", "/try"];
export const DEFAULT_LANDING_PAGE = "/";
export const authRoutes = ["/auth/signin", "/auth/signup", "/auth/error"];
export const protectedRoutes = ["/chat", "/settings", "try"];
export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/chat/home";
